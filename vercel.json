{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh -s -- -y && $HOME/.local/bin/uv export --no-dev --format requirements-txt > requirements.txt",
  "functions": {
    "api/*.py": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "regions": ["hnd1"]
}